---

- name: Change the hostname
  win_hostname:
    name: "{{ hostname }}-1"
  register: res

- debug:
    var: "{{ hostname }}"
- debug:
    var: "{{ vm_name }}"

- name: Reboot - hostname set
  win_reboot:
    msg: "Rebooting after setting hostname"
  when: res.reboot_required